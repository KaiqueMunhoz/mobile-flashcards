import {StyleSheet} from 'react-native'

export default StyleSheet.create({

    buttonSubmit : {
        marginTop: 25,
        marginLeft: 16,
        marginRight: 16,
    },

    textSubmit : {
        color: '#fff'
    }
})